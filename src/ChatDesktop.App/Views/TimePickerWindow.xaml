<Window x:Class="ChatDesktop.App.Views.TimePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="选择时间" Height="220" Width="320" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F4F6FA"
        Language="zh-CN">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="请选择时间 (HH:mm)" FontWeight="SemiBold" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Center">
      <ComboBox x:Name="HourComboBox" Width="80" Margin="0,0,8,0" />
      <TextBlock Text=":" VerticalAlignment="Center" Margin="0,0,8,0" />
      <ComboBox x:Name="MinuteComboBox" Width="80" />
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Style="{StaticResource MaterialDesignFlatButton}" Content="取消" Width="80" Margin="0,0,8,0" Click="OnCancelClicked" />
      <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="确定" Width="80" Click="OnConfirmClicked" />
    </StackPanel>
  </Grid>
</Window>
