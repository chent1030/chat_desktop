# AI服务配置
AI_API_URL=https://your-api-server.com/api/chat
AI_API_KEY=your-api-key-here

# 可选：两套AI Key（仅切换 Key，URL 不变）
AI_API_KEY_XIN_SERVICE=your-xin-service-api-key-here
AI_API_KEY_LOCAL_QA=your-local-qa-api-key-here

# 任务语音抽取专用 Key（推荐单独配置，避免与聊天等场景混用）
AI_API_KEY_TASK_EXTRACT=app-ZWCHs3FAdAxHjnKAgUyQwEY1
# 可选：任务语音抽取专用 URL / SSE（不填则复用 AI_API_URL / AI_SSE_URL）
AI_API_URL_TASK_EXTRACT=
AI_SSE_URL_TASK_EXTRACT=

# SSE接收地址（如果与发送地址不同，可选）
AI_SSE_URL=https://your-api-server.com/api/chat/stream

# WebSocket服务端点
WEBSOCKET_URL=wss://api.example.com/ws/tasks

# MQTT配置
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

MQTT_SESSION_EXPIRY_SECONDS=604800

# 可选：一次订阅多个主题（逗号/换行/分号分隔）；支持 {empNo} 或 $empNo 占位符
# 例如：MQTT_TOPICS=mqtt_app/tasks/{empNo}/email,mqtt_app/tasks/{empNo}/ass
MQTT_TOPICS=

# Optional: Windows external app paths (if URL schemes don't work)
#OUTLOOK_PATH_WINDOWS=C:\\Program Files\\Microsoft Office\\root\\Office16\\OUTLOOK.EXE
#DINGTALK_PATH_WINDOWS=C:\\Program Files (x86)\\DingTalk\\DingTalk\\DingtalkLauncher.exe

# 设备ID（首次启动时自动生成）
DEVICE_ID=

# 调试开关：true 时 Unify 接口走 Mock 数据
DEBUG=false

# Unify API（任务相关）
UNIFY_API_BASE_URL=https://cshzeroapi.uabcbattery.com/unify/v1/0
# 创建任务（POST，成功返回204）
UNIFY_API_CREATE_TASK_PATH=/aiTask/backendSaveTask
# 任务已读（POST，成功返回204）
UNIFY_API_TASK_READ_PATH=/aiTask/taskRead
# 任务完成（POST，成功返回204）
UNIFY_API_TASK_COMPLETE_PATH=/aiTask/taskCompleted
# 任务分页 列表（GET）
UNIFY_API_TASK_LIST_PATH=/aiTask/selectTaskPage
# 派发候选列表（返回数组：empName/empNo/workGroup/accessGroup）
UNIFY_API_DISPATCH_CANDIDATES_PATH=/userGroup/list
